自動提交並推送到 master 分支。

執行以下步驟：
1. 執行 `git status` 查看變更
2. 執行 `git add -A` 加入所有變更
3. 根據變更內容生成適當的 commit message（使用 conventional commits 格式）
4. 執行 `git commit` 提交變更
5. 執行 `git push origin master` 推送到遠端

如果沒有任何變更，告知用戶並結束。
